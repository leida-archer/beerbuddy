# Dependencies
node_modules/
.pnpm-store/

# Next.js
.next/
out/
build/
dist/

# TypeScript incremental build cache
*.tsbuildinfo

# Local env
.env
.env.local
.env.*.local
!.env.example

# Vercel
.vercel/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Tests
coverage/
.nyc_output/
playwright-report/
test-results/

# tool-stack local state
.tool-stack/

# Drizzle: commit BOTH migrations and meta snapshots — drizzle-kit
# uses meta/ to compute diffs between subsequent generations.

# Adapter run artifacts (LLM raw outputs, screenshots, PDFs cached during dev)
ingest/.cache/
ingest/.fixtures-tmp/

# Visual companion mockups (brainstorm skill)
.brainstorm/
.vercel

# Local scratch (screenshots, traces, throwaway outputs)
.tmp/

# Project-local MCP config (contains absolute home paths — per-machine)
.mcp.json
